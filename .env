# ----- DATABASE -----
DB_HOST=postgres
DB_PORT=5432
DB_USER=tracker
DB_PASSWORD=changeme
DB_NAME=trackerdb

# Full DB URL for tracker-server
TRACKER_DB_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ----- JWT -----
JWT_SECRET=4c67b22d5e8c47df73ded9c798ba5524fa96b049ec93a718172ac1ae1447771a
JWT_EXPIRES=3600
TRACKER_JWT_SECRET=${JWT_SECRET}

# ----- REDIS -----
REDIS_HOST=redis
REDIS_PORT=6379

# ----- SERVER CONFIG -----
ENV=development
LOG_LEVEL=info
SERVER_PORT=8000
TRACKER_SERVER_HOST=0.0.0.0
TRACKER_SERVER_PORT=${SERVER_PORT}

# ----- IP GEO (optional) -----
IPGEO_PROVIDER=ipapi
IPGEO_API_KEY=
TRACKER_IPGEO_PROVIDER=${IPGEO_PROVIDER}
TRACKER_IPGEO_API_KEY=${IPGEO_API_KEY}

# ----- Server environment -----
TRACKER_ENV=production
TRACKER_DOMAIN=localhost

